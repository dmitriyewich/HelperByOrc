# Codex Actions

- 2026-04-25: Expanded maximum diagnostics for the Windows 10 loading-screen/cursor investigation. Added startup environment inventory in `HelperByOrc/mod_app.*` (file fingerprints, PE metadata, proxy candidates, root/CLEO/MoonLoader/modloader/SAMPFUNCS scans, loaded-module snapshots with conflict tags), larger log message buffer in `HelperByOrc/debug_log.cpp`, SA:MP readiness diagnostics in `HelperByOrc/samp_api.*` / `samp_api/core/samp_api_core.inl` (global pointers, memory regions, CNetGame/pool/RakClient/chat/dialog fields, code bytes), explicit RakClient vtable calls (`Send=6`, `RPC=25`), and D3D/overlay diagnostics in `HelperByOrc/imgui_overlay.cpp` (GTA HWND `0x00C8CF88`, window details, Direct3DCreate9/dummy-device timing, module/RVA for Reset/Present/EndScene). Verified R3-1 addresses with Rizin against `C:\Games\SAMP\main\R3-1\samp.dll`; local Arizona log from `C:\Games\Arizona Games Launcher\bin\arizona\modloader\HelperByOrc\HelperByOrc.log` shows a healthy run: `sampInfo=0` clears, hooks install, Present renders. Updated `.gitignore`, `context.md`, `README.md`, forum `README.txt`, and this action log so `.codex/actions.md` is the only published `.codex` file. Verified `Release|Win32` build succeeds with 0 errors/warnings.

- 2026-04-23: Added targeted freeze-isolation diagnostics and startup gating for overlay input in `HelperByOrc/imgui_overlay.*` and `HelperByOrc/mod_app.*`. New `ImGuiOverlay` gate callback delays `WndProc` hook/input pipeline until SA:MP reaches full init (`ModApp` now tracks `sampUiPipelineReady_` via periodic `sampApi_.isSAMPInitilizeLua()` checks during refresh). While gated, cursor pipeline is skipped and logs a waiting marker; once ready, `WndProc` hook is installed lazily with explicit trace. Added timestamped probe markers around `Present`/`EndScene` detours (`[ui][probe] ... ts=... init=... gate=...`) and around `ModApp::Tick` refresh window (`[probe] Refresh begin/end ... sampReady(beforeHooks/afterHooks)`), so next user log can separate overlay-side freeze from external module stall. Verified `Release|Win32` build succeeds with 0 errors/warnings; no deploy.

- 2026-04-23: Refactored `HelperByOrc/main.cpp` to avoid heavy lifecycle work under loader lock: `DllMain` now only creates a shutdown event + bootstrap worker thread, while `ModApp::OnProcessAttach(...)` and normal `ModApp::Shutdown()` run on that worker outside `DllMain`. Added guarded fallback to synchronous attach if event/thread creation fails, plus detach signaling and explicit-unload wait (`WaitForSingleObject(..., 5000)`) before closing handles. Verified `Release|Win32` build succeeds with 0 errors/warnings; no deploy.
- 2026-04-23: Added early bootstrap lifecycle logging in `HelperByOrc/main.cpp`: `OutputDebugStringA` markers for `PROCESS_ATTACH`/`PROCESS_DETACH` and worker thread start/finish, plus file-log `debuglog::WriteInfo` checkpoints (`attach completed`, `waiting for shutdown signal`, `shutdown signal received`, fallback sync attach/shutdown paths). Switched UI logging defaults to full `info` in `HelperByOrc/ui_settings.*`: in-memory default (`logLevel_`), load fallback (`log_level` default string), and `ResetToDefaults()` now all use `Info`. Verified `Release|Win32` build succeeds with 0 errors/warnings; no deploy.

- 2026-04-20: Replaced binder **block** semantics with **require** semantics for game/chat UI conditions: `conditions` / `quick_conditions` gate run and quick-menu visibility when checked states are satisfied. Added `conditions_combine` and `quick_conditions_combine` (`require_all` | `require_any`) on hotkeys and `quick_conditions_combine` on folders; editor condition popups include AND/OR combo. Removed RMB-based UI cursor release in `HelperByOrc/mod_app.cpp` and RMB `MouseDrawCursor` suppression in `HelperByOrc/imgui_overlay.cpp` for working folder context menus on ПКМ. Updated `HelperByOrc/ui_settings.h` strings. Migrated `C:\Games\Arizona Games Launcher\bin\arizona\modloader\HelperByOrc\HelperByOrc.json` to new keys; updated `HelperByOrc/tools/generate_binder_test_config.py`. Verified `Release|Win32` build; no deploy.
- 2026-04-21: Implemented cursor-conflict hardening for overlay/chat/dialog ownership in `HelperByOrc/mod_app.cpp` and `HelperByOrc/imgui_overlay.cpp`. `ModApp::UpdateOverlayCursorMode` now computes one unified hold condition from `(ImGui UI open) OR (SA:MP chat open) OR (SA:MP dialog active)` with game-window focus, so chat/dialog no longer race against overlay cursor ownership. Added periodic cursor-mode reassert (`kCursorReassertIntervalMs=200`) while hold is active to recover when SA:MP/chat flows reset cursor mode behind our cache, removing the "chat close while ImGui open -> cursor drops/centers" failure. Switched ImGui software cursor rendering off (`io.MouseDrawCursor=false`) for a single visible cursor (game cursor only), eliminating double-cursor visuals. Pending `Release|Win32` verification build/runtime check.
- 2026-04-21: Added concise cursor-owner diagnostics in `HelperByOrc/mod_app.cpp` cursor trace line: now logs explicit source flags `chatOpen` and `dialogOpen` (alongside `wantsUi`/`chatOrDialog`/`shouldHold`) so runtime quickly shows which subsystem currently requests cursor hold, without introducing high-frequency extra log lines.
- 2026-04-21: Added log-noise throttle for cursor-mode reassert telemetry in `HelperByOrc/mod_app.cpp`: while periodic reassert (`Set_CursorMode mode=3`) remains active for robustness, repetitive `reassert=1` success messages are now emitted at a reduced cadence (`kCursorReassertTraceIntervalMs`), keeping transition diagnostics intact and making `HelperByOrc.log` readable during long UI sessions.
- 2026-04-21: Changed default UI log level to `Off` in `HelperByOrc/ui_settings.*`: startup fallback (`ui.log_level` missing), in-memory defaults, and `ResetToDefaults()` now all default to disabled logging. Users can still switch to `Error`/`Info` manually in Settings when needed.
- 2026-04-21: Rewrote forum-facing `README.txt` into a short, mass-user-friendly BLASTHACK BB-code post format (simple language, no bind examples per request, clear install/what-it-does/important notes blocks, and lightweight forum smilies).
- 2026-04-21: Documentation sync pass: updated local `context.md` and tracked `README.md` to reflect current cursor reassert log-throttle behavior and the new default UI log level (`off`), plus clarified test-config generator usage (`tools/generate_binder_test_config.py` with overridable `OUT` path for local modloader targets).

- 2026-04-20: Investigated bind drag/drop reliability between top-level folders (e.g. moving from `Тесты_HelperByOrc/*` into `Основные`) and applied a focused UI fix in `HelperByOrc/binder_module.cpp`: bind-row drag source now uses `ImGuiDragDropFlags_SourceNoDisableHover` (`BeginDragDropSource(...)`) so folder-tree drop targets keep hover state while dragging across panes. This addresses intermittent/non-immediate cross-root drops caused by hover suppression during drag. Pending verification build/runtime check.

- 2026-04-20: Completed remaining module logging pass for `HelperByOrc/app_config.cpp` and `HelperByOrc/minhook_utils.h`. Replaced all residual generic `debuglog::Write(...)` calls with explicit `debuglog::WriteInfo(...)` / `debuglog::WriteError(...)` (no legacy calls left in `HelperByOrc`). In `app_config.cpp`, added action-level lifecycle logs for `OnProcessAttach`, `Shutdown`, config-load success, and snapshot-save success; failure paths for read/parse/write/replace and empty config path now log as explicit errors. In `minhook_utils.h`, all MinHook failure/invalid-argument/rollback diagnostics now emit explicit `error` logs. Verified repository-wide `HelperByOrc` search is clean for `debuglog::Write(`, `Release|Win32` build succeeds, and lint is clean; no deploy.
- 2026-04-20: Completed combined "full debug log" pass for `HelperByOrc/binder_module.cpp` and `HelperByOrc/tags_module.cpp`. In `binder_module.cpp`, converted all remaining generic `debuglog::Write(...)` calls to explicit `debuglog::WriteInfo(...)` / `debuglog::WriteError(...)` (`quickmenu` state traces, cascade menu diagnostics, `DoSend via_samp` begin/ok/fail, binder log action). In `tags_module.cpp`, introduced action-level lifecycle/config logs: `OnProcessAttach` begin/done (with tag/custom-var counters), `Shutdown` begin/done, `SetSampApi` / `SetBinderModule` assignment traces, and `LoadConfig` / `SaveConfig` begin-done/queued traces. Verified no remaining `debuglog::Write(...)` in either module, `Release|Win32` build succeeds, and lint is clean; no deploy.
- 2026-04-20: Completed module-by-module "full debug log" pass for `HelperByOrc/samp_rak_hooks.cpp`: converted all remaining generic `debuglog::Write(...)` calls to explicit `debuglog::WriteInfo(...)` / `debuglog::WriteError(...)` in RakNet hook install flow (target address traces, install success/fail paths). Added action-level runtime logs for `SetSampApi`, `Refresh` gating decisions (missing API, waiting for SA:MP, unsupported version, install request), `Shutdown` begin/end, and `CleanupHooks` begin/end. Added explicit `error/info` logs for delayed install (`RakClientInterface` not ready) and fatal install branches (`samp.dll` missing, RPC offset missing, null vtable). Verified `Release|Win32` build succeeds and lint is clean; no deploy.
- 2026-04-20: Completed module-by-module "full debug log" pass for `HelperByOrc/samp_hooks.cpp`: converted all remaining generic `debuglog::Write(...)` calls to explicit `debuglog::WriteInfo(...)` / `debuglog::WriteError(...)` in hook-install flow (target address traces, install success/fail paths). Added action-level runtime logs for `SetSampApi`, hotkey-block callback assignment, `Refresh` gating decisions (missing API, waiting for SA:MP, unsupported version, install request), `Shutdown` begin/end, and `CleanupHooks` begin/end. Verified `Release|Win32` build succeeds and lint is clean; no deploy.
- 2026-04-20: Completed module-by-module "full debug log" pass for `HelperByOrc/samp_api.*`: in `samp_api/core/samp_api_core.inl` added action logs for module attach/refresh/backend mode transitions and upgraded version-detection logs to explicit `WriteInfo`/`WriteError`; added centralized error-flow tracing in `SetError`/`ClearError` (`SampApi error` / `error cleared`). In `samp_api/chat/samp_api_chat.inl` converted all remaining generic `debuglog::Write(...)` traces to explicit level-based calls (`WriteInfo` for flow, `WriteError` for failure paths), including remote-player/ped resolution, send-chat branches, and `_chat.asi` discovery/SEH failures. In `samp_api/dialog/samp_api_dialog.inl` and `samp_api/raknet/samp_api_raknet.inl` added action-level begin/ok logs for cursor mode, dialog close/submit, chat input open/close, list/editbox operations, Rak client resolve, RPC/packet send, and dialog-response send outcome. Verified `Release|Win32` build succeeds; no deploy.
- 2026-04-20: Added targeted runtime debug instrumentation for ImGui/overlay audit checkpoints in `HelperByOrc/imgui_overlay.cpp` and reduced cursor-log spam in `HelperByOrc/mod_app.cpp`. New traces are throttled and cover: duplicate same-frame `RenderFrame` calls, D3D9 `Reset` begin/success/failure, `CreateStateBlock(D3DSBT_ALL)` failures, pass-through `WM_SETCURSOR` with active UI cursor, `ReleaseCapture` on UI-hold end, and skipped cursor-mode apply when SA:MP module/version is unavailable. Verified `Release|Win32` build succeeds; no deploy.
- 2026-04-20: Implemented single-pass render guard in `HelperByOrc/imgui_overlay.cpp` (`ShouldRunRenderPassThisTick`) and wired it into both `PresentDetour` and fallback `EndSceneDetour` so only one ImGui render/update pass runs per device per system tick. Removed the old duplicate-render detector from `RenderFrame`; duplicate passes are now skipped with throttled telemetry (`[ui] skipped duplicate render pass in same tick ...`). Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-20: Reverted the per-tick render guard after runtime logs showed visible ImGui flicker and massive skip batches (`skipped=100+` up to `1168`) caused by multiple valid presents in the same millisecond. Restored the simpler, stable render policy in `HelperByOrc/imgui_overlay.cpp`: `PresentDetour` remains the primary render path, `EndSceneDetour` renders only fallback when `Present` is unavailable (`!originalPresent_`). Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-20: Added an ImGui capture-stability follow-up in `HelperByOrc/imgui_overlay.cpp`: when `WantsUiCursor()` is true, `RenderFrame` now calls `ImGui::SetNextFrameWantCaptureMouse(true)` right after `ImGui::NewFrame()` to reduce transient `WantCaptureMouse=0` gaps observed as `wnd_btn_anomaly` in runtime logs. Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-20: Trimmed noisy post-render diagnostics in `HelperByOrc/imgui_overlay.cpp`: added `kPostRenderHealthTraceIntervalMs` (3.5s), gated `[ui] diag post-render` output to only useful frames (`capture/hover/input active` or anomaly), and limited deep internal snapshots to anomaly-only cases (`wantsUiCursor && !WantCaptureMouse`). Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-20: Expanded debug telemetry coverage in `HelperByOrc/imgui_overlay.cpp` across the checklist points: per-second render path counters (`present`/`endscene` calls vs actual render source), throttled logs for non-primary render skips, shutdown-time detour observations, game-window DPI trace on init, explicit `RestoreWindowProc` success/failure logs, `CleanupImGui` and overlay `Shutdown` begin/end logs, plus slow-frame traces for both `full_ui` and idle passes. Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-20: Added focused popup-anomaly diagnostics and a cleaner production-debug profile in `HelperByOrc/imgui_overlay.cpp`: introduced `UiDebugProfile` with `ProductionDebug` defaults (slower telemetry cadence, higher slow-frame threshold, reduced skip noise), added `IsPopupTransitionNoCaptureExpected(...)` and updated `[ui] diag post-render` to distinguish hard anomaly vs popup-transition (`popupTransitionExpected`) when `WantCaptureMouse=0` with UI cursor active (e.g. `##Popup` transitional frame). `wnd_btn_anomaly` now ignores this expected popup-transition case to reduce false positives. Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-20: Implemented configurable two-level logging with UI control: added `debuglog::Level` (`Off/Error/Info`) plus `SetLevel/GetLevel` and `WriteError/WriteInfo` in `HelperByOrc/debug_log.*`; `Write(...)` now auto-classifies existing messages as `Error` when format text contains error/fail markers and otherwise `Info`, so legacy logs participate in level filtering without rewriting every callsite. Extended `UiSettings` with persisted `ui.log_level` (`off|error|info`) and added the Settings-tab combo (`Off`, `Error`, `Info`) in `HelperByOrc/mod_app.cpp`; changing/resetting settings now applies runtime log level immediately. Updated `.cursor/rules/orcoutfit-workflow.mdc` with a logging policy rule for touched/new runtime flows (action logs + explicit error logs, respecting global level). Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-20: Module-by-module logging migration started with `HelperByOrc/imgui_overlay.cpp`: replaced all remaining generic `debuglog::Write(...)` calls in this module with explicit `debuglog::WriteInfo(...)` / `debuglog::WriteError(...)` by branch intent (runtime actions vs failures). This makes `imgui_overlay` fully aligned with the new `Off/Error/Info` policy. Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-20: Completed module-by-module logging migration for `HelperByOrc/mod_app.cpp`: replaced all `debuglog::Write(...)` usages with explicit `debuglog::WriteInfo(...)` / `debuglog::WriteError(...)` (attach/shutdown/cursor-flow/state transitions as `Info`, SA:MP cursor apply failure and logo resource/texture load failures as `Error`). Verified there are no remaining generic `debuglog::Write(...)` calls in `mod_app.cpp`. Rebuilt `Release|Win32` successfully; no deploy.

- 2026-04-20: Reverted the uncommitted Arizona dialog integration in `HelperByOrc/samp_api.*` that tried to drive dialogs through `core.asi` / `vorbisFile.dll`; dialog reads, edit-box writes, list selection, and submit/close now use only the standard SA:MP dialog path again. Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-20: Compacted duplicated UI/helper code in `HelperByOrc/binder_module.cpp` and `HelperByOrc/tags_module.cpp` (shared condition-popup rendering, shared running-bind lookup helpers, shared searchable token-picker list/copy flow). Switched `HelperByOrc/HelperByOrc.vcxproj` `Release|Win32` to a more aggressive size-oriented profile based on `WeaponsOutFit`: removed `imgui_demo.cpp` from the active build, disabled release debug info/RTTI/security cookies/SDL checks, enabled string pooling + `/Gw` + LTCG, and changed optimization to `MinSpace/Size`. Rebuilt `Release|Win32` successfully; `HelperByOrc.asi` shrank from `2732544` to `2327552` bytes (`-404992`, about `14.82%`). Existing `Release\\HelperByOrc.pdb` is stale from the older build and was not regenerated.
- 2026-04-20: Reduced the quick menu default size in `HelperByOrc/binder_module.cpp` from `320x360` to `214x277`, which is roughly 1.5x narrower and 1.3x shorter while preserving the same scaling/centering flow for both quick-menu layouts. Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-20: Style readability pass for the current dark-blue theme: in `HelperByOrc/mod_app.cpp` strengthened selectable/table contrast (`Header*`, `TableHeaderBg`, `TableBorder*`, `TableRowBgAlt`) without replacing the theme; in `HelperByOrc/binder_module.cpp` reworked bind-table row styling so selected rows fully override zebra striping, hovered rows get a lighter overlay, selected rows boost launch/bind text readability, and bind names are left-aligned instead of manually centered inside the cell. Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-20: Reverted `ModApp::ApplyMainStyle` back to the previous darker blue-accent theme after the slate-blue experiment. Kept the neutral section-card follow-up (`DrawSectionCard` now uses the active theme child background instead of a hardcoded color) and restored the old centered `SelectableTextAlign`. Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-20: Reworked the main Dear ImGui theme in `HelperByOrc/mod_app.cpp` toward a slate-blue desktop look based on the provided reference: brighter graphite window surfaces, thinner borders, roomier padding, subdued blue active states for tabs/menus/list selections, left-aligned selectable text, and style-driven section-card backgrounds instead of the older harsher dark-blue theme. Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-20: Read `.codex/actions.md`; refreshed `context.md` (local) plus `README.md`/`README.txt` for `_chat.asi` chat path (`Set_ChatInputText`, `process_chat_input`, insert-vs-open-chat), quick-menu default size, and post-revert UI theme/table readability. Committed `HelperByOrc` sources + README and pushed to `origin/main` (excluded `context.md`, root `.gitignore`, `.cursor/`, `refecence/`). Verified `Release|Win32` build.
- 2026-04-19: Split chat-input bind behavior in `BinderModule::Impl::DoSend`: method `Вставить в чат` now calls `Set_ChatInputText(..., false, ...)` and no longer opens/closes chat around the insert, while method `Открыть чат` still opens chat and inserts text into the input buffer. Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-19: Extended the Arizona `_chat.asi` path for background send: `process_chat_input(...)` now first tries runtime discovery `###input -> xref -> wrapper -> buffer -> writer -> submit`, writes UTF-8 into the ImGui input buffer, then calls the discovered submit routine with argument `1` to emulate Enter without opening chat. If `_chat.asi` is missing or submit discovery fails, it falls back to the standard SA:MP `CDXUTEditBox::SetText` + `CInput::ProcessInput` route. Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-10: Read `context.md`.
- 2026-04-10: Updated `context.md` with rules: only `build -> release`, keep Codex action memory in `.codex/actions.md`, use canonical version names `R5-1` and `DL-R1`.
- 2026-04-10: Updated `.gitignore` to exclude `.codex/`.
- 2026-04-10: Inspected current version model and offset tables in `HelperByOrc/samp_api.h`, `HelperByOrc/samp_api/core/samp_api_core.inl`, and `HelperByOrc/samp_rak_hooks.cpp`.
- 2026-04-10: Confirmed current code still uses legacy internal alias `R5_2` for the canonical `R5-1` bucket.
- 2026-04-10: Confirmed `C:\Games\SAMP\main` contains canonical version folders only: `R1`, `R2`, `R3`, `R3-1`, `R4`, `R4-2`, `R5-1`, `DL-R1`.
- 2026-04-10: Collected SHA-256 hashes for local `samp.dll` files to use as scan anchors if needed.
- 2026-04-10: Verified scanner tool availability: `C:\Program Files\Rizin\bin\rizin.exe` -> `rizin 0.8.2`.
- 2026-04-10: No project code changes made yet. Next step is to clarify ideal user inputs before scanning `samp.dll`.
- 2026-04-10: Confirmed `R4` and `R4-2` entry points from local DLLs. Current `HelperByOrc` mapping is swapped and must be corrected during the version-support update.
- 2026-04-10: User requested manual per-version verification instead of automatic matching. Next scans will be done version by version.
- 2026-04-10: Manually verified RakNet offsets per version from local `samp.dll`: `handleRpc`, `stringWriteEncoder`, `stringReadDecoder`, `compressorPtr` for `R2`, `R3`, `R4`, `R4-2`, with `R3 == R3-1` and `R4-2 == R5-1` for those routines.
- 2026-04-10: Manually verified structural offsets from local `samp.dll`: `rakclient_interface`, `SAMP_INFO_OFFSET_Pools_Veh`, and `SAMP_COLOR_OFFSET`; corrected `R2` `SAMP_COLOR_OFFSET` to `0x216380` after disassembly, not the older Lua value `0x216378`.
- 2026-04-10: Updated project code to use canonical internal version name `R5_1`, expanded offset tables to `R1/R2/R3/R3-1/R4/R4-2/R5-1/DL-R1`, and added a broader remote-data ped fallback so newer layouts do not depend only on bundled legacy `sampapi` headers.
- 2026-04-10: Saved a compact reverse-engineering summary in `.codex/samp-offsets-2026-04-10.md`.
- 2026-04-10: Updated `context.md` and `README.md`/`readme.md` to pin the current verified `MSBuild.exe` path: `C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe`.
- 2026-04-10: Audited the expanded offset tables against `HEAD` plus manually verified `R2/R3/R4/R4-2` values; no accidental column shifts or value drift were found in `entryPoint`, `main_offsets`, or `kRakOffsets`.
- 2026-04-11: Read `context.md` and `.codex/actions.md` before preparing binder stress tests.
- 2026-04-11: Rewrote external runtime config `C:\Games\Arizona Games Launcher\bin\arizona\modloader\asi\HelperByOrc.json` with a binder stress suite: 59 binds across activation, send methods, multi-input, tags, orchestration, dialogs, conditions, and quick menu visibility cases.
- 2026-04-11: Verified the rewritten external `HelperByOrc.json` parses successfully via `ConvertFrom-Json`; confirmed `binder.quick_menu_activation_mode = toggle` and `binder.hotkeys.Count = 59`.
- 2026-04-11: Added separate `command_confirmation` support for binder hotkeys in `HelperByOrc/binder_module.cpp`, reusing the existing confirm/cancel keys and pending-confirmation flow that was previously trigger-only.
- 2026-04-11: Updated binder UI/localization in `HelperByOrc/ui_settings.h` so the editor now shows `Требовать подтверждение по команде` plus a dedicated `Дожидаться подтверждения или отклонения по команде` checkbox.
- 2026-04-11: Built `Release|Win32` successfully after the command-confirmation change and updated test bind `A10 Command Confirm Wait` in `C:\Games\SAMP\GTA SAMP\scripts\HelperByOrc.json` to use `command_confirmation.enabled = true` with `text_confirmation.enabled = false`.
- 2026-04-11: Fixed duplicate text-trigger activation for slash-commands in `HelperByOrc/binder_module.cpp` by ignoring command-like messages in `OnOutgoingChat`; slash commands now trigger text binds only through `OnOutgoingCommand`.
- 2026-04-11: Fixed duplicate regex trigger activation for incoming chat lines with prefixes in `HelperByOrc/binder_module.cpp` by routing `prefix + text` and plain `text` through a single per-message matcher that triggers each bind at most once.
- 2026-04-11: Aligned incoming text-trigger routing with the Lua reference by removing binder subscription to RakNet `ClientMessage` in `HelperByOrc/binder_module.cpp`; incoming text triggers now come only from the chat AddEntry hook to avoid double activation from parallel backends.
- 2026-04-11: Added a unified incoming message pipeline in `HelperByOrc/incoming_message_router.*` that collects AddEntry and RakNet `ClientMessage`, merges cross-source duplicates in a short window, and feeds binder through one canonical message stream from `mod_app.cpp`.
- 2026-04-11: Rewired binder incoming text triggers to subscribe to `IncomingMessageRouter` instead of raw hooks; verified `Release|Win32` builds successfully with the new router integrated.
- 2026-04-11: User established a new workflow rule in `context.md`: never copy or deploy binaries from `HelperByOrc\Release\` anywhere automatically; any copy of release artifacts must require a separate explicit user command.
- 2026-04-11: Investigated remaining duplicate incoming text-trigger activation (`A5` exact and `A6` regex) and confirmed the matcher path in `HelperByOrc/binder_module.cpp` already triggers each hotkey at most once per `IncomingMessageEvent`; the remaining duplication is upstream of `MatchTextTrigger`.
- 2026-04-11: Finished and corrected the recent cross-source dedupe in `HelperByOrc/incoming_message_router.cpp`: complementary `AddEntry`/`RakNet` duplicates are now suppressed even when they arrive after the merge window, while same-source repeats remain distinct.
- 2026-04-11: Added debug-log tracing in `HelperByOrc/incoming_message_router.cpp` for every received `AddEntry`/`RakNet` message plus router stages (`recv`, `queue_pending`, `merge_ready`, `timeout_ready`, `drop_recent_duplicate`, `dispatch`); rebuilt `Release|Win32` successfully without copying release artifacts anywhere.
- 2026-04-11: Parsed the runtime log from `C:\Games\SAMP\GTA SAMP\scripts\HelperByOrc.log`; for both `hb exact` and `hb regex ...` the router emits exactly one merged `dispatch`, so the remaining duplicate activation happens downstream of `IncomingMessageRouter`.
- 2026-04-11: Verified there is only one loaded game-side binary candidate in `C:\Games\SAMP\GTA SAMP\scripts\HelperByOrc.asi`, no duplicate `A5/A6` hotkeys in `scripts\HelperByOrc.json`, and no duplicate toast texts reused by other binds.
- 2026-04-11: Added binder-side debug tracing in `HelperByOrc/binder_module.cpp` for router registration, incoming message handling, trigger match, enqueue, bind start, and `DoSend`; rebuilt `Release|Win32` successfully without deploying the binary.
- 2026-04-11: Fixed binder hotkey polling robustness in `HelperByOrc/binder_module.cpp` by syncing the runtime key tracker with `GetAsyncKeyState` every tick; this is meant to restore binds like `Ctrl+1`, `Q+E+R`, and repeat combos even when the WndProc key stream is incomplete.
- 2026-04-11: Moved final ImGui overlay rendering to a new `IDirect3DDevice9::Present` hook in `HelperByOrc/imgui_overlay.*` while keeping `EndScene` as initialization/fallback; goal is for ImGui windows to render above SA:MP chat and dialog layers.
- 2026-04-11: Raised binder quick menu and its submenus to the front each frame in `HelperByOrc/binder_module.cpp` and switched the compact cascade picker to a full-width `Selectable` list to avoid squeezed choice buttons in `I5 Cascade /hb_input_cascade`.
- 2026-04-11: Rebuilt `Release|Win32` successfully after the hotkey, Present-hook, quick-menu z-order, and cascade layout fixes; release artifacts were not copied anywhere.
- 2026-04-11: Corrected the non-compact input-dialog list path for cascade binds in `HelperByOrc/binder_module.cpp`: search boxes now use full-width item sizing and regular cascade choices render as full-width buttons like the Lua reference, which should fix the squeezed `I5 Cascade /hb_input_cascade` options.
- 2026-04-11: Read `context.md` and `.codex/actions.md` before investigating MoonLoader cursor behavior for ImGui.
- 2026-04-11: Inspected current native cursor/input routing in `HelperByOrc/imgui_overlay.cpp`, `HelperByOrc/mod_app.cpp`, and `HelperByOrc/samp_api/dialog/samp_api_dialog.inl`; noted that `inputCaptureActive_` currently follows `ImGuiIO::WantTextInput`, while non-text open UI falls back to SA:MP cursor mode `LOCKCAM`.
- 2026-04-11: Inspected Lua references `C:\Games\Arizona Games Launcher\bin\arizona\moonloader\lib\mimgui\init.lua`, `...\lib\imgui.lua`, and `...\lib\SFlua\037-r1\init.lua`; confirmed `mimgui` uses MoonLoader `showCursor()`/`lockPlayerControl()` separately, and `ImGui.SetMouseCursor(...)` only changes the ImGui cursor shape, not OS/game cursor ownership.
- 2026-04-11: Reverse-inspected `C:\Games\Arizona Games Launcher\bin\arizona\MoonLoader.asi` with Rizin. Confirmed `showCursor(bool show, [bool lockControls=false])` is more than `USER32!ShowCursor`: it toggles internal patches/device state, shows/hides the Win32 cursor, and remembers whether it locked controls itself so hide only unlocks when the earlier show call used `lockControls=true`. No project code changes made.
- 2026-04-11: Checked the old Lua reference project under `C:\Games\CODEX\HelperByOrc\src`; it uses `mimgui` defaults for the main window, sets `HideCursor` only for special cases like toasts or conditional chat UI, and does not use `LockPlayer = true` anywhere in the project sources.
- 2026-04-11: Updated native ImGui cursor ownership in `HelperByOrc/imgui_overlay.*` and `HelperByOrc/mod_app.cpp`: interactive UI now requests one stable SA:MP cursor mode (`LOCKCAM`) through a unified `WantsUiCursor()` path, text-input capture stays separate from cursor ownership, `WM_SETCURSOR` is swallowed while UI owns the cursor, and redundant same-state `Set_CursorMode(...)` calls were removed.
- 2026-04-11: Built `Release|Win32` successfully after the ImGui cursor/input-routing fix. Release artifacts were not copied anywhere.
- 2026-04-11: Read `context.md` and `.codex/actions.md` before reverse-inspecting `C:\Games\Arizona Games Launcher\bin\arizona\MoonLoader.asi` for `sampProcessChatInput(zstring text)`, per user request and without code changes.
- 2026-04-11: Reverse-inspection result for `MoonLoader.asi`: the Lua name `sampProcessChatInput` is registered from string `sampProcessChatInput` at `0x10198030`, bound through wrapper code at `0x100ee880`, which marshals the text via helper `0x1005adb0` into a shared argument buffer and then invokes generic opcode dispatcher `0x101266e0` with opcode `0x0C8F` (`3215`). This means MoonLoader does not implement `sampProcessChatInput` here as a standalone native SA:MP routine; it forwards into the opcode engine, so this path is not a good reference if we want to avoid a `SAMPFUNCS` dependency.
- 2026-04-11: Read `context.md` and `.codex/actions.md` before reverse-inspecting `C:\Games\Arizona Games Launcher\bin\arizona\_chat.asi` for the Enter key message-submit path, per user request and without code changes.
- 2026-04-11: Reverse-inspection result for `_chat.asi` Enter submit path: keyboard handler `0x1000AFDB` reacts to `WM_KEYUP (0x101)` with `VK_RETURN (0x0D)` when chat is open (`[chat+0x14E0] != 0`) and calls submit routine `0x100176D2(1)`. That routine clones the current input buffer, runs pre-submit filters, tries local slash-command handlers, then falls through to `0x10017BD8 -> 0x100040E4`, which dispatches to direct SA:MP calls: regular text uses `GetModuleHandleW("samp.dll") + 0x5820` (`CInput_SendSay` for R3-1), slash commands use `+ 0x69190` (`CInput_Send`), and afterward it closes chat input through `samp.dll + 0x68E10`. This means `_chat.asi` Enter-submit is ultimately a wrapper around direct SA:MP routines, not a separate network backend.
- 2026-04-11: Read `context.md` and `.codex/actions.md` before investigating binder send method `Через SA:MP` and the toast `Не удалось отправить текст в SA:MP`.
- 2026-04-11: Added targeted debug logging in `HelperByOrc/binder_module.cpp` and `HelperByOrc/samp_api/chat/samp_api_chat.inl` for binder method `Через SA:MP`: the log now records the outgoing text preview, branch (`command` vs `chat`), resolved pointers/addresses, and the exact `SampApi::lastError()` when direct SA:MP send fails.
- 2026-04-11: Built `Release|Win32` successfully after the new `Через SA:MP` debug logging change. Release artifacts were not copied anywhere.
- 2026-04-11: Replaced Arizona `_chat.asi` byte-signature scanning in `HelperByOrc/samp_api.cpp` with runtime discovery based on `_chat.asi` imports/control flow: locate `GetModuleHandleW("samp.dll")`, recover the wrapper that jumps to `samp.dll + CInput_Close`, then identify the Arizona submit routine by its call to that close-wrapper plus the internal chat-state globals (`std::string` object and dirty flag). No fixed `_chat.asi` RVA/signature is required anymore for `process_chat_input`.
- 2026-04-11: Updated `HelperByOrc/samp_api/chat/samp_api_chat.inl` so `process_chat_input(...)` now tries the discovered Arizona submit path when `_chat.asi` is present, logs the Arizona-specific fallback reason if that path is unavailable, and otherwise falls back to the standard `CDXUTEditBox::SetText -> CInput::ProcessInput` route. Built `Release|Win32` successfully after the change; release artifacts were not copied anywhere.
- 2026-04-11: Fixed the first Arizona runtime-discovery regression in `HelperByOrc/samp_api.cpp`: the `_chat.asi` wrapper for `samp.dll + CInput_Close` uses `add eax, imm; pop esi; jmp eax`, so the locator now accepts a short gap between `add eax, offset` and the final `call/jmp eax`. Added one-line runtime discovery debug output with the recovered `_chat.asi` stages (`GetModuleHandleW` IAT, `samp.dll` literal, `getSampModule`, `closeWrapper`, `processInput`, `stringObject`, `dirtyFlag`). Built `Release|Win32` successfully after the fix; release artifacts were not copied anywhere.
- 2026-04-11: Fixed a second Arizona runtime-discovery regression in `HelperByOrc/samp_api.cpp`: function scans now stop at the next discovered local function start instead of reading a flat 96/512-byte window, because the previous version could spill into the next `_chat.asi` function and misidentify `getSampModule` as the `SendSay` wrapper (`0x...2C46E`). Rebuilt `Release|Win32` successfully after the boundary fix; release artifacts were not copied anywhere.
- 2026-04-11: Switched Arizona `process_chat_input(...)` to prefer a discovered `_chat.asi` submit-text helper instead of raw `std::string` buffer writes. The new runtime-discovery path in `HelperByOrc/samp_api.cpp` now also finds a function that references the same Arizona chat string object and performs `push 0; call processInput`, and `HelperByOrc/samp_api/chat/samp_api_chat.inl` calls that helper first when available. This is intended to avoid failures on long texts when the Arizona input buffer still has small-string capacity (`<= 15`). Built `Release|Win32` successfully after the change; release artifacts were not copied anywhere.
- 2026-04-11: Fixed `submitText` discovery for Arizona `_chat.asi`: the needed helper is a data-xref-style function (for example near `0x...2223E`), not necessarily a normal rel32-call target, so `HelperByOrc/samp_api.cpp` now scans executable sections directly for `push 0; call processInput`, then reconstructs the helper start from the nearest preceding `ret` boundary and validates it against the same Arizona chat string object. Rebuilt `Release|Win32` successfully after the fix; release artifacts were not copied anywhere.
- 2026-04-11: Corrected the Arizona `submitText` start recovery in `HelperByOrc/samp_api.cpp`: the helper around `0x...2223E` was still being missed because the nearest previous `ret` boundary was not reliable there, so discovery now walks backward from `push 0; call processInput` to the nearest `push ebp; mov ebp, esp` prologue and validates that window against the same Arizona chat string object. Rebuilt `Release|Win32` successfully after the prologue-based fix; release artifacts were not copied anywhere.
- 2026-04-11: Confirmed the discovered Arizona `_chat.asi` submit helper uses `push 0; call processInput`, which explains why programmatic sends were bypassing Enter-only observers such as chat history updates. Updated `HelperByOrc/samp_api.cpp`, `HelperByOrc/samp_api.h`, and `HelperByOrc/samp_api/chat/samp_api_chat.inl` so `process_chat_input(...)` now temporarily flips that helper's mode byte to `1` only for our call, restoring true Enter-like behavior while keeping runtime discovery and fallback logic intact. Rebuilt `Release|Win32` successfully; release artifacts were not copied anywhere.
- 2026-04-11: After runtime testing showed the patched Arizona enter-submit no longer reached the server, inspected `_chat.asi` again and noted the real Enter path runs with chat input already opened (`CInput_Opened` context). Updated `HelperByOrc/samp_api/chat/samp_api_chat.inl` so Arizona `submitText` enter-submit now temporarily opens `CInput` before the helper call and closes it afterward if Arizona leaves it open. Rebuilt `Release|Win32` successfully; release artifacts were not copied anywhere.
- 2026-04-11: Narrowed the Arizona reverse-engineering scope to the two real building blocks the user asked for: `_chat.asi` pure set-input logic and `_chat.asi` Enter-submit logic. Found that `0x1000D826` is the core setter for the internal chat input (`data.100920D8` + dirty flag), `0x10022413` is its one-argument wrapper that prepares text and then calls that setter, and the real Enter path remains `processInput(1)` at `0x100176D2`. Reworked `HelperByOrc` to stop using the composite `submitText` helper and instead prefer `set input text -> processInput(1)` for Arizona chat runtime discovery. Rebuilt `Release|Win32` successfully; release artifacts were not copied anywhere.
- 2026-04-11: Validated the Arizona `setInputText` false positive with Rizin on `C:\Games\Arizona Games Launcher\bin\arizona\_chat.asi`: the broken runtime discovery target was `_chat.asi + 0x22389`, while the real wrapper that calls `0x1001409C` and then `0x1000D826(..., 0)` starts at compiler-style prologue `_chat.asi + 0x22413`. Kept the `RecoverFunctionStartNearCallsite(...)` fix in `HelperByOrc/samp_api.cpp` so runtime discovery can recover compiler/EH-style function starts, then rebuilt `Release|Win32` successfully without copying release artifacts.
- 2026-04-12: Removed the custom Arizona chat runtime-discovery/integration layer from `HelperByOrc/samp_api.*`; chat input now uses only the standard SA:MP edit-box and `CInput::ProcessInput` path, dialog chat-text reads no longer consult the custom Arizona buffer, and `Release|Win32` builds successfully without copying release artifacts.
- 2026-04-19: Clarification: the 2026-04-11 binder duplicate-trigger investigation (`dispatch` once in `IncomingMessageRouter`, duplication downstream) was resolved in code afterward; the older journal lines were left stale. Current `binder_module.cpp` handles one merged `OnIncomingMessage` per ready item; each hotkey is evaluated once per normalized/prefixed branch (`MatchTextTrigger` → `TryDispatchTextTriggerMatch`).
- 2026-04-19: Verified there is no remaining Arizona `_chat.asi` discovery or integration in `HelperByOrc` sources (grep clean); chat uses standard SA:MP paths only.
- 2026-04-19: Fixed `SAMP_REMOTEPLAYERDATA_OFFSET == 0` handling for legacy `CRemotePlayer` layouts (`R1` / `R3` / `R3-1`): added `SampApi::ResolveRemotePlayerData`, switched `GetHealthAndArmour` to `ResolveRemotePlayer` + that helper, and fixed `GetPlayerPedPointer` remote-data fallback so offset `0` aliases the `CRemotePlayer*` instead of reading `m_pPed`. Compared layouts against vendored BlastHack `SAMP-API` headers (`CRemotePlayer` has no separate remote-data pointer on those builds); opened `C:\\Games\\SAMP\\main\\R1\\samp.dll` / `R3-1\\samp.dll` in Rizin (`iz~RemotePlayer` yields nothing on stripped binaries). Built `Release|Win32` successfully; release artifacts not copied.
- 2026-04-19: Initial overlay cursor hardening added foreground-gated `Set_CursorMode(LOCKCAM)`, `ImGuiOverlay::GetGameWindow`, shutdown cleanup (`ClipCursor(nullptr)`, `ReleaseCapture`, SAMP cursor reset), plus swallowing `WM_SETCURSOR` with a forced arrow. That swallow/`ClipCursor` path was superseded later the same day by the WeaponsOutFit-aligned behavior in the following journal line.
- 2026-04-19: Regenerated `C:\Games\SAMP\GTA SAMP\scripts\HelperByOrc.json` from `HelperByOrc/tools/generate_binder_test_config.py`: full Russian labels, 11 test folders (hotkeys, all send methods 0–9 on Ctrl+F1–F10, text triggers, confirmations, commands, tags/wait, input dialogs, all 9 game conditions, quick menu visibility, stress 25+30 toasts, edge cases), `tags.custom_vars` sample, `quick_menu_activation_mode` set to `toggle` and `quick_menu_hotkey` to Alt+X; 77 hotkeys. User can re-run the script to refresh the file.
- 2026-04-19: Aligned overlay cursor behavior with `C:\Games\CODEX\WeaponsOutFit` (reference `source\overlay.cpp`): `ImGuiIO::MouseDrawCursor` is set while the overlay wants a UI cursor and the right mouse button is not held (temporary look/camera passthrough); `WM_SETCURSOR` is no longer swallowed or forced to an arrow (`IsMouseMessage` excludes it); removed `ClipCursor` from `ModApp::UpdateOverlayCursorMode` while keeping SA:MP `Set_CursorMode(LOCKCAM)` when UI holds focus. Built `Release|Win32` successfully (`HelperByOrc\Release\HelperByOrc.asi`); no deploy.
- 2026-04-19: Fixed ImGui focus/input stuck after closing the quick menu without clicks: when neither the main menu nor auxiliary overlay is visible, `RenderFrame` previously skipped `ImGui::NewFrame()`, so windows that called `SetNextWindowFocus()` (quick menu) left stale focus/hover until another full overlay frame. Added `AdvanceImGuiFrameWithoutUi` (empty NewFrame→Render pipeline each Present) when UI is hidden so ImGui state advances every frame. Built `Release|Win32` successfully; no deploy.
- 2026-04-19: Added `[ui]` debug traces to `HelperByOrc.log`: render-path transitions (`idle_tick` / `full_ui`), throttled post-render ImGui IO snapshot (`after_present_ui`), SA:MP cursor pipeline (`ModApp::UpdateOverlayCursorMode`), quick menu state in `BinderModule::Impl::Tick`, `WM_LBUTTON*` / `WM_RBUTTON*` / `WM_MBUTTON*` path in `OverlayWndProc`, and non-`WM_MOUSEMOVE` `binder` swallows. Rebuilt `Release|Win32`.
- 2026-04-19: Fixed main-window clicks dead after quick-menu-from-idle using log-backed diagnosis (`WantCaptureMouse=0`, `AnyItemHovered=0` while eating mouse): Win32 backend can leave `MousePos` invalid after `WM_MOUSELEAVE` when `ImGui_ImplWin32_UpdateMouseData` does not apply the `GetCursorPos` fallback (`MouseTrackedArea` / focus edge cases). On first overlay frame after an all-idle stretch (`enteredOverlayFromNone`), `ImGuiOverlay::RenderFrame` now calls `io.ClearInputMouse()` and `AddMousePosEvent` from `GetCursorPos`+`ScreenToClient` after `ImGui_ImplWin32_NewFrame`. Rebuilt `Release|Win32`.
- 2026-04-19: Quick menu cascade: submenu windows were iterated in `std::map` key order (lexicographic), so a sibling path like `.../Item2` could sort after `.../Item/Child` and be drawn last, placing a shallower panel above a deeper one and stealing clicks from the third level. `DrawQuickMenu` now sorts `quickMenuSubmenuPaths` by path depth (`/` count), shallow-to-deep so the deepest submenu is drawn last and stays on top. Rebuilt `Release|Win32`.
- 2026-04-19: Quick menu bind rows used `ImGui::MenuItem` outside a menu/popup context; hover on folder `Selectable` worked but bind clicks could fail (inconsistent hit testing vs `Selectable`). Replaced bind rows with `ImGui::Selectable` + optional shortcut text + `BeginDisabled` when the hotkey is disabled. Rebuilt `Release|Win32`.
- 2026-04-19: Quick menu cascade auto-close + flaky binds: parent submenu stayed `shouldClose=true` while the mouse only hovered a **child** panel (`IsWindowHovered` false on parent), so `hoveredAnyMenuWindow` dropped intermittently and branches timed out (`kQuickMenuSubmenuCloseGraceSeconds`). After the child-keep pass, if any open path is a proper descendant (`path` + `/` prefix), force `shouldCloseSubmenu[ancestor]=false` and clear its deadline. When any cascade is open, if `io.MousePos` is invalid, re-inject `AddMousePosEvent` from `GetCursorPos`+`ScreenToClient` on the main viewport HWND. Rebuilt `Release|Win32`.
- 2026-04-19: Documentation and Git policy: updated `README.md`, added forum-oriented `README.txt` (BB-code); expanded `context.md` with GitHub publication rules (sources + build profile only), remote URL, and removal of `.gitignore`/`context.md` from `origin`; updated `.cursor/rules/orcoutfit-workflow.mdc` (artifact path `HelperByOrc\\Release\\HelperByOrc.asi`, GitHub scope). Removed root `.gitignore` and `context.md` from git index (`git rm --cached`) while keeping local copies; committed and pushed `main` to `https://github.com/dmitriyewich/HelperByOrc`.
- 2026-04-19: Quick menu: removed `submenuTriggerHovered` path that called `removeSubmenuState` for any `shouldClose` submenu when *any* folder row was hovered — moving the mouse from a folder row toward a child panel often crossed a **sibling** folder row, wiping the open branch before the user could click binds. Folder hover for open/keep-open now uses `ImGui::IsMouseHoveringRect` (z-order aware) plus `IsItemHovered(AllowWhenBlockedByActiveItem)` instead of raw `MousePos` vs AABB. `GetCursorPos`+`ScreenToClient` mouse re-injection while the quick menu is open (not only when submenus already exist). Rebuilt `Release|Win32`.
- 2026-04-19: Quick menu **rewrite**: removed the multi-window cascade (`quickMenuSubmenuOpen` / extra `Begin` panels, grace timers, depth sort). One main window per session; each root tab uses `BeginChild` + recursive `TreeNodeEx` for folders (`DefaultOpen` only for depth 0 under the tab), `Selectable` rows for binds unchanged. Dropped `kQuickMenuSubmenuCloseGraceSeconds` and `kIconAngleRight`. Rebuilt `Release|Win32`.
- 2026-04-19: Quick menu styles: added `quick_menu_style` to binder config and a settings dropdown (`Стиль быстрого меню`) with Style 1 (tree) and placeholder Style 2 (WIP). Mouse wheel in quick menu is reserved for scrolling while holding the quick-menu hotkey; tab switching by wheel requires Ctrl. Rebuilt `Release|Win32`.
- 2026-04-19: Implemented full Style 2 quick menu from scratch using ImGui menu API only (`BeginMenuBar` + nested `BeginMenu` + `MenuItem`) with right-side cascading submenus and no custom multi-window cascade logic. Style 2 is now selectable in settings and uses folder recursion directly in menu hierarchy; bind click closes quick menu and enqueues action. Updated Style 2 UI text from WIP to active label. Rebuilt `Release|Win32`.
- 2026-04-19: Style 2 UX pass: enforced "folders first" order in menu recursion, added separators between folder block and bind block, and added stable unique IDs for root/folder/bind menu items (`##qm_style2_*`) while keeping visible icons in labels. Rebuilt `Release|Win32`.
- 2026-04-19: Style 2 visibility fix attempt: removed dependency on `BeginMenuBar` and rendered root `BeginMenu` entries directly in quick-menu window body (still pure `BeginMenu` + `MenuItem` cascade). Kept folders-first + separators + stable IDs. Rebuilt `Release|Win32`.
- 2026-04-19: Style 2 visibility fix attempt: removed dependency on `BeginMenuBar` and rendered root `BeginMenu` entries directly in quick-menu window body (still pure `BeginMenu` + `MenuItem` cascade). Kept folders-first + separators + stable IDs. Rebuilt `Release|Win32`.
- 2026-04-19: Runtime logs showed `visibleRoots=1` but no successful root menu-open events in style 2 menu-context variants. Reworked style 2 to a dedicated quick-menu window layout: left pane lists visible folders hierarchically, right pane shows selected folder content (child folders first, then separator, then bind rows). Selection path persists while quick menu is open (`quickMenuStyle2SelectedPath`). Rebuilt `Release|Win32`.
- 2026-04-19: Renamed quick-menu style label to `Стиль 2: двухпанельный` / `Style 2: two-pane`.
- 2026-04-19: Added style 3 (`quick_menu_style = style3`) as a new from-scratch BeginMenu/MenuItem implementation (without `BeginMenuBar`/`BeginMainMenuBar`): separate style branch in `DrawQuickMenu` opens an anchored root popup and renders full recursive folder/bind hierarchy through `BeginMenu` + `MenuItem` only, with folders-first ordering and separators before bind list. Rebuilt `Release|Win32`.
- 2026-04-19: Adjusted style 3 to the exact ImGui "tool window + menu bar + menus" pattern (`ImGuiWindowFlags_MenuBar`, `BeginMenuBar`, `BeginMenu`, `MenuItem`) and added `[ui][style3]` runtime traces (`frame`, `BeginMenuBar=false`, `root/child menu open`) to diagnose visibility issues in-game. Rebuilt `Release|Win32`.
- 2026-04-19: Style 3 follow-up for empty menu labels: kept `BeginMenuBar` template but switched style3 menu captions to safe ASCII runtime labels (`Root N`, `Folder N`, `Bind #N`) with stable IDs, preserving folder-first ordering and bind shortcuts/enabled-state. Added trace `BeginMenuBar=true` and kept root/child-open logs. Rebuilt `Release|Win32`.
- 2026-04-19: Quick menu style-3 stabilization pass: removed hold grace-period experiment, moved style 3 from `BeginMenuBar` dependency to window-body menu rendering, added diagnostic traces for root/node visibility, fixed focus stealing by replacing per-frame `SetNextWindowFocus()` with one-shot `quickMenuFocusPending`, restored real folder/bind names, and re-aligned style 3 filtering to style 1/2 (`FolderVisibleInQuickMenu`, `FolderHasVisibleQuickEntries`, `QuickEntriesForFolder`). Rebuilt `Release|Win32`.
- 2026-04-19: Set quick menu default style to `style3` in binder defaults/config fallback (`NormalizeQuickMenuStyle`, initial field value, `LoadConfig` default), updated `README.md` and forum `README.txt` to document style 3 as default and the current GitHub publication preference to keep `HelperByOrc/external` vendored in repository.
- 2026-04-19: Quick menu: removed two-pane style; enum is now `Tree` + `Cascade` only. Config key `quick_menu_style` saves as `tree` / `cascade`; legacy `style2`, `style3`, `2`, `3` load as cascade. UI label `Стиль 2: каскадное меню` / `Style 2: cascaded menu`. Runtime debug tag `[ui][qm_cascade]`. Updated `README.md`, `README.txt`, local `context.md`.
- 2026-04-19: Quick menu tree style: replaced per-tab `BeginChild(..., Borders)` scroll region with `BeginListBox` + `PushID(tab)` for framed list presentation (ImGui 1.92 wrapper). Rebuilt `Release|Win32`.
- 2026-04-19: Read `context.md` and `.codex/actions.md`; updated `context.md` build section for `msbuild` on `PATH` (full-path fallback noted) and expanded overlay bullet on pre-`NewFrame` mouse resync when Win32 reports invalid `MousePos`.
- 2026-04-19: Diagnosed `HelperByOrc.log` (`WantCapMouse=0` with `eatMouse=1`, `wantsUiCur=1`): `WndProc` eats mouse while ImGui has no useful hover; quick-menu `AddMousePosEvent` in `renderCallback` runs after `ImGui::NewFrame` (too late for the current frame). Initial fix: pre-`NewFrame` resync when `IsMousePosValid` failed; later expanded (see next line).
- 2026-04-19: Mouse issue persisted in-game; log still showed `WantCapMouse=0` while `IsMousePosValid` stayed true (stale numeric coords). Patched vendored `imgui_impl_win32.cpp` `ImGui_ImplWin32_UpdateMouseData`: treat foreground as focused when it is a **child** of `bd->hWnd`, and run `GetCursorPos` fallback when `MouseTrackedArea != 0` but `!IsMousePosValid(&io.MousePos)`. `imgui_overlay.cpp` now always re-injects mouse from `GetCursorPos`/`ScreenToClient` before `NewFrame` on every overlay-visible frame (not only invalid-flag). Rebuilt `Release|Win32`.
- 2026-04-19: Compared `refecence/test.cpp` with `imgui_overlay` WndProc: reference swallows mouse only when `io.WantCaptureMouse`; we used `WantsUiCursor() || WantCaptureMouse`, which matches log lines `eatMouse=1` + `WantCapMouse=0`. Aligned `OverlayWndProc` mouse blocking with ImGui’s own `WantCaptureMouse` only (keyboard unchanged: text capture + `WantCaptureKeyboard`). Rebuilt `Release|Win32`.
- 2026-04-19: Log still showed `WantCapMouse=0` with `eatMouse=0` after WndProc-only change — hit-test drift. Root cause: `ModApp::RenderUi` set `io.FontGlobalScale` / `ApplyMainStyle` **after** `ImGui::NewFrame()` in `ImGuiOverlay::RenderFrame`, so input/hover used the wrong scale vs drawn widgets. Added `ImGuiOverlay::SetPrepareFrameCallback` + `ModApp::PrepareUiForImGuiNewFrame` invoked after `ImGui_ImplWin32_NewFrame` and before `NewFrame` (also in `AdvanceImGuiFrameWithoutUi`). Restored `wantsMouseCapture = wantsUiCursor || io.WantCaptureMouse`. Rebuilt `Release|Win32`.
- 2026-04-19: User report: UI works when window looks “active” (brighter), fails when “inactive” (dimmer) with mouse still over ImGui. `DrawOverlay` draws `##binder_toasts` **after** quick menu with `BgAlpha=0` but **without** `ImGuiWindowFlags_NoMouseInputs`, so the transparent toast stack still participated in mouse/hover/focus and could sit above interactive UI. Added `ImGuiWindowFlags_NoMouseInputs` to toast flags (ImGui 1.92: pass-through mouse per `imgui.h`). Rebuilt `Release|Win32`.
- 2026-04-19: Quick menu: popup host (`OpenPopup`/`BeginPopup` `##helperbyorc_qm_host`), removed per-frame `BringWindowToDisplayFront`, `BringWindowToFocusFront` + `syncOsMouseToImGui` on `quickMenuFocusPending` for tree and cascade; dropped duplicate `AddMousePosEvent` after `NewFrame` in `DrawQuickMenu`. `imgui_overlay`: disabled `NavEnableKeyboard` / `ConfigNavCaptureKeyboard` for overlay. Local `.gitignore`: stop ignoring vendored `external/{imgui,memwrapper,plugin-sdk,SAMP-API}`; removed nested `.git` in those four dirs; committed ~4.3k files vendored snapshot + source/README; **did not** commit `context.md`, root `.gitignore`, `.cursor/`, `refecence/`. Pushed `main` to `https://github.com/dmitriyewich/HelperByOrc` (`312c01f`). Updated `context.md` (local) and README for GitHub scope clarification. Verified `Release|Win32` build.
- 2026-04-19: Read `context.md` and `.codex/actions.md` for the chat-bind task. Confirmed binder send methods `Вставить в чат` and `Открыть чат` still route through `BinderModule::Impl::DoSend` cases `4` and `5`, both calling `SampApi::Set_ChatInputText(...)`.
- 2026-04-19: Reintroduced a narrow Arizona `_chat.asi` input integration for `Set_ChatInputText(...)` only: at runtime it now discovers `###input -> xref -> InputText wrapper -> writable input buffer -> writer` directly from the loaded `_chat.asi` image, uses that writer first for bind methods `Вставить в чат` / `Открыть чат`, and falls back to the standard SA:MP `CDXUTEditBox::SetText` path when `_chat.asi` is absent or discovery fails. Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-19: Fixed `_chat.asi` chat-input mojibake (`1���`-style output): the discovered Arizona writer expects UTF-8 text for the ImGui input string, while the standard SA:MP edit-box path still expects game encoding (`cp1251`). `Set_ChatInputText(...)` now prepares both forms from one transformed UTF-8 source, feeds UTF-8 to the `_chat.asi` writer, and keeps `cp1251` only for the fallback `CDXUTEditBox::SetText` route. Rebuilt `Release|Win32` successfully; no deploy.
- 2026-04-22: Added runtime setting for `CDamageManager_ApplyDamage` behavior toggle (without unhooking): new UI checkbox `apply_damage_protection` in settings persists to `ui` config, `SampHooks::SetApplyDamageProtectionEnabled` controls whether the detour blocks non-vehicle component ranges, and `ModApp` synchronizes the flag on startup/reset/runtime change. Verified with `msbuild "HelperByOrc.slnx" /p:Configuration=Release /p:Platform=Win32` — build succeeded (0 errors, 0 warnings), artifact `HelperByOrc\Release\HelperByOrc.asi`.
- 2026-04-22: Updated docs for the new toggle: `README.md` now documents `apply_damage_protection` in recent changes, and local `context.md` `ui` section/example now include `log_level` + `apply_damage_protection`. Committed source/docs (`e5c35f7`) and pushed `main` to `origin` (`https://github.com/dmitriyewich/HelperByOrc.git`).
- 2026-04-22: Added root GitHub Actions workflow `.github/workflows/build-release-win32.yml` for CI builds of `HelperByOrc` (`Release|Win32`): triggers on `workflow_dispatch`, `push/pull_request` to `main`, and `release.published`; uploads `HelperByOrc/Release/HelperByOrc.asi` as run artifact (`HelperByOrc-Release-Win32`, 30 days) and attaches the same `.asi` to GitHub Release assets for published releases.
- 2026-04-22: Adjusted `.github/workflows/build-release-win32.yml` trigger policy per user request: removed automatic `push`/`pull_request` builds; workflow now runs manually via `workflow_dispatch` and on `release.published` only.
- 2026-04-22: Fixed GitHub Actions build failure in `.github/workflows/build-release-win32.yml`: quoted MSBuild property argument as `"/p:Configuration=Release;Platform=Win32"` so PowerShell does not split on `;`, and added `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` to preempt the Node 20 deprecation warning path for JavaScript actions.
- 2026-04-22: Fixed GitHub CI toolset mismatch (`MSB8020`) by overriding build property in `.github/workflows/build-release-win32.yml` to `PlatformToolset=v143` for GitHub runner builds (`/p:Configuration=Release;Platform=Win32;PlatformToolset=v143`), avoiding dependency on unavailable `v145` toolset in hosted Windows image.
